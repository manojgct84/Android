<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="popupTheme">@style/MenuTheme</item>
    </style>

    <style name="MenuTheme" parent="Theme.MaterialComponents.Light">
        <item name="android:itemBackground">@color/menuColor</item>
        <item name="android:textColor">@color/menuText</item>
    </style>
    <!--This is place the navigation bar as under the status base and status base becomes transparent -->
    <style name="AppTheme.NoActionBar">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

</resources>
